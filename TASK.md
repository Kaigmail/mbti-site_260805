# Codex 任务卡 #3 · 前端重做(按设计稿 V1 山野活力风)

## 背景
项目:mbti-site_260805(徒步&溜达风格测试,类MBTI 娱乐向)。
前端已有一版可运行实现,但**视觉风格已由产品方重新选定**,需要按设计稿重做前端视觉。

## 设计参照(最重要)
**打开并仔细研究这个文件**:`sketches/001-shan-ye-vital/index.html`
它就是"山野活力风"设计稿(首页/测试页/结果页三屏),包含完整 CSS 和交互。你的任务是**把这个视觉风格完整搬到正式前端**(public/ 下),并接上真实数据和真实 API。

设计稿关键特征:
- 背景浅绿 `#f4f8f3`,文字深绿 `#1f3d2b`
- 主色森林绿 `#2d8a4e`,强调橙 `#ff8c42`
- 大圆角(16-22px)、胶囊按钮、3D 按压效果(box-shadow 下沉)
- 首页:emoji 图标行(🥾🏔️⛺)+ 大标题 + 副标题 + 白卡片说明 + 橙色大按钮
- 测试页:进度"第 X/20 题" + 圆角进度条 + 白卡片题目 + 绿框选项(带圆形 A/B/C 角标)+ 上一题
- 结果页:角色图(圆角白边)+ 大号 TURBO + 中文名 + 虚线分享标签 + 短标题 + 关键词绿色胶囊 + 4 张卡片(徒步人格/轻松吐槽/你可能会这样/行动建议)+ 重新测试绿色按钮
- 移动端优先(所有样式在 420px 宽度内好看),桌面端居中卡片

## 必须保留的(不许动)
- `server/`、`data/`、`docs/` 目录:**禁止修改**
- 后端 API 契约不变:GET `/api/questions`、POST `/api/result`、POST `/api/save`
- 页面功能:一次一题、20题、进度显示、返回上一题修改、结果页展示、重新测试
- 角色图路径:`/images/characters/<CODE>.webp`(静态目录已就位)
- 不要执行 git 操作

## 要做的
1. 重做 `public/` 下的前端(HTML/CSS/JS),视觉完全对齐设计稿
2. 首页/测试页/结果页三个页面(或 SPA 单页)均可
3. 结果页必须**适合手机截图分享**:一屏内尽量完整、留白充足、卡片分区
4. 保留并接入真实数据:题目从 `/api/questions` 拿,提交到 `/api/result` 拿结果,展示所有字段(英文标签/中文名/分享标签/短标题/3关键词/描述/吐槽/2条"你可能会这样"/2条行动建议),重新测试按钮调 `/api/save` 前要重新走答题流程
5. 结果保存流程:现有页面流程(提交答案→拿结果→保存)必须保留可用

## 验收标准(逐条自测并报告)
1. `node server/index.js` 启动后,浏览器打开首页:标题/副标题/说明/开始按钮与设计稿一致
2. 开始测试:第 1/20 题显示,进度条正确,选项 A/B/C 可点
3. 答完 20 题出结果页:角色图显示、TURBO 暴走风火轮、全部文案字段齐全
4. 第 1 题答完点"上一题"能返回修改,修改后继续
5. 移动端视口(375px)下页面无溢出、按钮可点、结果页适合截图
6. 颜色/圆角/按钮风格与设计稿一致(浅绿底、森林绿+橙、大圆角)

## 报告格式
完成/失败逐条列出验收结果;如果有截图路径一并给出。不要只说"完成了",给实际验证证据。
