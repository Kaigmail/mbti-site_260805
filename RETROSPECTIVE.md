# 项目复盘:徒步&溜达风格测试网站(mbti-site_260805)

> 日期:260809 ｜ 作者:Ada(Hermes) ｜ 项目:类 MBTI 徒步人格测试网站
> 链接:GitHub `Kaigmail/mbti-site_260805` ｜ 线上:`https://y6ndnkcbq4.coze.site` ｜ 数据:Supabase

## 一、项目概况

娱乐向类 MBTI 徒步人格测试:20 道题 → 15 对维度计分 → 12 种徒步人格标签 → 趣味报告 + 数据统计页。**双 Agent 协作开发**(Hermes 产品/验收 + Codex 编码),最终部署扣子编程(coze.site),数据库 Supabase,全流程上线。

---

## 二、发现问题

| # | 问题 | 发现时机 |
|---|---|---|
| 1 | 设计阶段看不到 UI,只能等全部做完才预览 | 前端 V1 完成、用户首次体验后 |
| 2 | Codex 写完代码后"自我验收"环节必崩 | 前端任务收尾时 |
| 3 | 匹配算法不公平:小画像标签永远输给大画像标签 | 12 标签全覆盖验收时 |
| 4 | 新版 Supabase 拒绝直接关闭行级安全 | 接入数据库保存时 |
| 5 | 扣子部署三次失败,构建/运行环境不兼容 | 部署阶段 |
| 6 | 线上保存失败:环境变量一个都没注入 | 上线复测时 |

## 三、探索思路

每个问题都先问"为什么",再验证,不直接改:

1. **设计看不到** → 借鉴设计行业"先出效果图再施工"→ 用纯 HTML 做 2-3 个设计变体,浏览器截图对比,用户拍板后再让 Codex 照做
2. **Codex 浏览器崩溃** → 读崩溃日志(0xC0000135)→ 手动启动同一浏览器验证(能启动)→ 排除"缺 DLL"和"双项目抢浏览器"→ 定位为 Codex 沙箱限制;手动复现 + 排除法
3. **匹配不公平** → 构造干净测试用例(贪心法:让目标标签命中最高、其他最低)→ 发现 2 个特征 vs 3 个特征的上限差 → 改命中率计分;发现"死维度"(对立面从未出现)→ 跳过;发现"平局白送分" → 严格胜出
4. **RLS 拒绝** → 读报错(violates row-level security policy)→ 确认新版强制 RLS → 改用 anon 放行策略(标准做法)
5. **部署失败** → 读完整日志 → 定位 `GLIBC_2.38 not found` → 追根:better-sqlite3 原生模块预编译下载超时 → 现场编译 → 与运行环境 glibc 不兼容 → 彻底移除原生依赖 + 双端口兼容 + DB_TYPE 自动判断
6. **线上保存失败** → 测 `/api/save` 报 `database is not initialized` → 查线上 JS 确认是新代码 → 日志 `injected env (0)` → 确认环境变量没配 → 手动配 3 个变量后恢复

## 四、解决方案(关键决策)

| 问题 | 方案 | 为什么 |
|---|---|---|
| 设计看不到 | 设计稿先行:3 变体对比 → 用户拍板 → Codex 照做 | 改设计稿 10 分钟,改代码 1 小时 |
| Codex 自验崩 | 分工固化:Codex 写,Ada 独立验收(浏览器走全流程) | 它说"好了"不当数,自己跑才算 |
| 匹配不公平 | 命中率+命中数计分、跳过死维度、平局不算命中 | 每个标签都有公平的获胜路径 |
| RLS | anon 放行策略(insert/select)而非关闭 RLS | 符合新版平台安全模型 |
| 部署失败 | 移除 better-sqlite3(纯 JS 依赖)、双端口 3000/5000、DB_TYPE 自动判断 | 云端零编译,构建=运行环境解耦 |
| 环境变量 | 手动新建 3 个变量(DB_TYPE/SUPABASE_URL/PUBLISHABLE_KEY) | 扣子不会自动配,必须手动 |

## 五、实际解决过程中遇到的情况(坑与插曲)

### 踩坑记录(按时间)

1. **附件来源乌龙**:Kai 说"上传了附件",Ada 没收到却去 Downloads 瞎找,找到的文件居然 MD5 一致(虚惊),但流程错了 → **教训:没收到附件就直接问,不许自己找**(已写入记忆)
2. **Codex 内置浏览器 GPU 崩溃**(exit 2,0xC0000135):写代码没事,一开浏览器验收就崩,重试 6 次全挂 → 手动启动同一浏览器正常 → **沙箱限制,不是缺 DLL** → 任务卡里明确"不要用浏览器验收,用 curl/读文件"
3. **Codex 更新后路径失效**:`Codex/bin/<hash>/codex.exe` 的 hash 目录随版本变(8/7 从 d7e8094c 变 cfac6bda)→ exit 127 → **不要硬编码路径,用 ls 找当前版本**
4. **node 后台启动静默失败**:git-bash 非 pty 后台跑 node+dotenv 直接 exit 1 无日志 → **node 服务器后台启动必须 pty=true**
5. **Supabase 强制 RLS**:`disable row level security` 无效,插入被拒 → 改用 anon 策略
6. **better-sqlite3 云端水土不服**:optional 依赖照样装;prebuild 超时 → 现场编译 → 运行环境 glibc 2.38 缺失 → 彻底移除
7. **扣子环境变量**:日志 `injected env (0)` = 一个都没注入;平台 AI 不会帮你配 → **手动新建 + 重新部署才生效**
8. **扣子端口**:FaaS 在 5000 端口探测应用,平台未注入 PORT → 双端口监听兜底

### 做得好的地方

- **三步确认制**:PRD → 计划 → 确认 → 动手,每次需求都整合确认后再开工
- **红线保护**:任务卡写死"不许动 server/data/docs",git diff 验收确认零改动
- **文案零改动**:数据提取用脚本 + 校验(20 题/15 对维度/12 标签逐字段),不靠 AI 转录
- **独立验收**:Codex 自述全当没听见,自己浏览器全流程 + 12 标签用例 + 边界测试
- **先展示后保存**:结果页先渲染报告,保存异步后台做,失败也不清空报告——用户要求的细节,写进了计划再实现
- **设计稿先行**:3 个风格变体对比,用户 30 秒拍板,避免做完再返工
- **协作留痕**:TASK.md 任务卡 + WORK_LOG.md 协作日志,每个交接有记录,复盘有据可查
- **部署前体检**:README、敏感信息 grep、依赖兼容、端口适配,一次排查减少三次失败中的两次
- **密钥安全**:只在 .env,不进 git,不上传压缩包;Publishable/Secret 分开用

### 还可以更好的地方

- 部署前应该**主动**让用户确认环境变量配置(当时给了清单,但用户第一次部署时没填)
- 第一次打包 zip 后应该**明确标注版本**(v1 含 better-sqlite3,v2 修复),用户混淆了旧包
- Supabase 表结构设计时没有预判 RLS 政策(新版平台默认行为变了)
- 统计页的 PROBE 测试数据混入真实数据,虽清理了,但测试应尽量用独立环境

## 六、复盘与反思

### 1. 双 Agent 协作的黄金分工(验证有效)

```
Hermes(产品/验收):需求→PRD→任务卡→独立验收→部署配置→留痕
Codex(编码):照任务卡写代码,不碰决策
```

关键:**验收权在 Hermes**。Codex 的浏览器自验能力在这台机器上不可用,但这不阻碍流程——它写,我验,用户拿到的永远是"已验证"的版本。

### 2. "本地能跑 ≠ 线上能跑"是最大教训

三次部署失败全部源于**环境差异**:原生模块(glibc)、环境变量(0 注入)、端口(5000)。对策三件套:
- 依赖只留纯 JS(零编译)
- 配置全部走环境变量(DB_TYPE 自动判断)
- 兼容平台默认探测(双端口)

### 3. 设计先行把返工成本压到最低

"先看设计再写码"让用户在设计阶段就参与,前端一次通过,没有返工。**这是下一个项目必须保留的流程**(已写入技能库)。

### 4. 用户亲自踩坑的价值

Kai 手动建环境变量时才发现"扣子 AI 配不了,必须手动"——这类平台细节,文档和 AI 都可能漏,实操才知道。**复盘的意义就是把实操经验沉淀下来,让别人不再踩。**

## 七、沉淀(已入库)

- 技能更新:`vibe-coding-workflow`(设计稿先行 + 分阶段预览)、`codex`(Windows 坑:路径变化/浏览器崩溃)
- 踩坑记录:已同步 `💻 编程开发/踩坑记录.md`
- 项目索引:已更新(已完成 🟢)
- 可复用资产:`docs/supabase_setup.sql` + `supabase_policy.sql`(任何 Supabase 项目直接复用)
