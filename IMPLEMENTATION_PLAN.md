# 实现计划 · 徒步&溜达风格测试 V1

> 日期:2026-08-05 ｜ 作者:Hermes(Ada) ｜ 状态:**待 Kai 确认** ｜ 依据:PRD V1.1(已确认)

---

## 1. 架构总览(技术选型)

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | **Node.js + Express** | 扣子编程环境支持 npm;轻量;与前端同语言,交接简单 |
| 前端 | **原生 HTML/CSS/JS(3 个页面)** | 零构建工具、部署最稳;项目规模小(3 页面),不需要 React/Vue 这类框架,也最大程度避免"本地能跑、扣子构建失败" |
| 数据 | **JSON 数据文件**(`questions.json` 题目+计分、`labels.json` 12 标签全文案) | 从附件精确提取,程序只读数据,文案零改动(红线) |
| 数据库 | **适配层**:本地 SQLite(`better-sqlite3`)↔ 扣子内置数据库 | 环境变量切换(`DB_TYPE`),上线换扣子库,业务代码零改动 |
| 静态资源 | 12 张角色图(压缩为 webp) | 结果页展示 |

**核心设计原则**:能用简单方案就不用复杂方案——原生 JS 零构建、数据库适配层隔离、数据与代码分离。目标是"本地跑通 = 线上能跑"。

## 2. 文件结构

```
D:\VibeCoding\mbti-site_260805\
├── PRD.md                        # 产品需求(已确认)
├── IMPLEMENTATION_PLAN.md        # 本计划
├── TASK.md                       # Codex 任务卡(每次派活更新)
├── WORK_LOG.md                   # 双 Agent 协作日志(复盘用)
├── docs\assets\                  # 原始素材归档
│   ├── Asset_Hikingman_Test&Lable.md
│   ├── 徒步人格角色方阵排版方案与提示词.md
│   ├── 徒步人格角色生成提示词.md
│   └── characters\               # 12 张角色图(原始 PNG)
├── data\
│   ├── questions.json            # 20 题 + 选项 + 计分维度
│   └── labels.json               # 12 标签全文案(含特征维度画像)
├── server\
│   ├── index.js                  # Express 入口(静态托管 + API)
│   ├── scoring.js                # 计分:30 维度 → 15 对倾向
│   ├── matching.js               # 方案 A:特征画像加权匹配
│   └── db.js                     # 数据库适配层(SQLite / 扣子库)
├── public\
│   ├── index.html                # 首页
│   ├── test.html                 # 测试页
│   ├── result.html               # 结果页
│   ├── css\style.css             # 样式(移动端优先)
│   ├── js\main.js                # 页面逻辑
│   └── images\characters\*.webp  # 压缩后角色图
└── package.json
```

## 3. 分阶段任务

### 阶段 0:数据与素材准备(Hermes 做)
1. 脚本从附件提取 `questions.json` / `labels.json`(20 题 × 3 选项、计分维度、12 标签全文案)
2. 写校验脚本:题目 20/选项 3/维度对 15/标签 12 × 7 字段,全部通过才算数据合格
3. 角色图压缩:23MB PNG → webp,单张 < 300KB(移动端加载性能)
4. 建立 `WORK_LOG.md` 协作日志

### 阶段 1:后端(Codex,任务卡驱动)
1. Express 服务器:`public/` 静态托管 + 两个 API
   - `POST /api/result`(入参:20 个选项)→ 计分 + 匹配 → 返回标签和得分
   - `POST /api/save`(标签 + 得分)→ 写数据库
2. `scoring.js`:30 个维度累计 → 15 对比较取倾向(平局取预设侧 = 每对 A 侧)
3. `matching.js`:12 标签特征维度命中计数,最高分者胜;同分按预设顺序;全 0 兜底(取总分最高维度所在标签)
4. `db.js`:SQLite 实现 + 扣子数据库适配接口(环境变量切换)

### 阶段 2:前端(Codex,任务卡驱动)
1. 首页:标题 / 副标题 / 说明 / 开始按钮(大按钮,移动端友好)
2. 测试页:一题一屏、进度条(第 X/20 题)、选择自动下一题、可返回修改
3. 结果页:角色图 + 英文标签 + 中文名 + 短标题 + 3 关键词 + 描述 + 吐槽 + 你可能会这样 ×2 + 行动建议 ×2 + 重新测试;卡片式、留白充足、**一屏内截图友好**
4. 响应式:375px(手机)→ 桌面;结果页角色图按标签显示

### 阶段 3:验证(Hermes 独立验证 + 双审)
1. 本地全流程:首页 → 20 题 → 结果页 → 重新测试
2. 边界用例:全选 A、全随机、中途返回改答案、维度平局
3. **12 标签全覆盖**:构造 12 组已知答案,逐一验证命中对应标签
4. 数据库验证:结果写入、重启不丢
5. 触发双审(新增 > 200 行 + 要部署公网):Hermes 走查 Codex 代码(`git diff` + 实际运行 + 边界测试)

### 阶段 4:部署(与 Kai 协作)
1. 推到 GitHub(仓库名待定,建议 `mbti-site_260805`)
2. 扣子编程:GitHub 授权 → 导入项目 → 自动构建
3. 部署配置:构建命令 / 端口 / 开启数据库 / 生产环境变量(如有)
4. 部署 → 线上验证:URL 可访问、全流程答题、结果入库

## 4. 关键算法落地(方案 A)

1. 每题选项 → 对应维度 +1(共 30 个维度,分数表见附件)
2. 15 对维度两两比较 → 倾向侧(平局取预设侧 A)
3. 对 12 个标签,统计其特征维度(PRD §5.2 表)命中数
4. 取命中数最高的标签;同分按附件标签出现顺序(预设优先级);理论全 0 时兜底取"得分最高的维度对"所在标签
5. 结果页展示该标签的完整文案 + 对应角色图

## 5. 风险与应对

| 风险 | 应对 |
|---|---|
| 扣子内置数据库的具体接入方式未知 | 阶段 0 查扣子文档 + 用最小验证项目试通;`db.js` 适配层隔离,接不上也不影响其他功能 |
| 扣子构建命令 / 端口要求 | 官方支持"构建指令、端口"配置,部署时按扣子要求填写 |
| 12 张图 23MB 过大,加载慢 | 阶段 0 压缩为 webp,单张 < 300KB |
| 匹配算法效果不直观 | 12 组已知答案用例强制验证;若某标签无法命中,调特征画像并记录 |
| 文案错漏(红线) | 数据脚本提取 + 校验脚本双保险;Codex 不许改动文案 |
| 本地 SQLite 与扣子库行为差异 | 只存"结果表"这一张简单表,适配层接口最小化(insert/select),切换成本极低 |

## 6. 验证标准(全部通过才算完成)

- [ ] `npm start` 本地全流程:首页 → 20 题 → 结果页,角色图正常显示
- [ ] 返回修改答案后,结果按新答案正确更新
- [ ] 12 组标签用例全部命中对应标签
- [ ] 结果成功写入数据库,重启服务不丢失
- [ ] 375px 手机宽度排版正常,结果页截图一屏完整
- [ ] 扣子编程部署成功,线上 URL 全流程可用,结果入库
- [ ] `WORK_LOG.md` 完整记录每个交接(时间 / Agent / 任务 / 结果)

## 7. 协作安排(双 Agent,复盘用)

- **Hermes**:产品经理(PRD / 计划 / 任务卡 / 验收 / 双审 / commit / 部署协调)
- **Codex**:专职开发(阶段 1、2,`codex exec --sandbox workspace-write`,任务卡自包含)
- 交接流程:任务卡 → Codex 干活 → Hermes 独立验证 → 双审(触发条件:>200 行 / 部署上线)→ Hermes 收尾 commit
- 全程记录在 `WORK_LOG.md` + git 提交记录,供 Kai 复盘

## 8. 已确认(Kai,2026-08-05)

1. **GitHub 仓库名**:`mbti-site_260805`(导入扣子编程用)
2. **部署域名**:用扣子默认分配(后续 Kai 自行修改),本阶段不处理

---

*确认后进入开发阶段(阶段 0 立即开始,不需要等 Codex)。*
