# V2 实现计划 · 接入 Supabase 数据统计

> 版本:V2.0 ｜ 日期:2026-08-05 ｜ 作者:Hermes(Ada) ｜ 状态:**待 Kai 确认**
> 关联:PRD.md V1.1(已确认)、TASK.md(任务卡 #1-#3 已完成)

---

## 1. 目标

1. 用户完成测试后,结果**保存到 Supabase**(替代/补充本地 SQLite)
2. 自动创建数据表(不用手动建库)
3. 新增**数据统计区域**:总人数、各角色人数、占比、角色分布图
4. 保存动作**不拖慢用户体验**(先展示报告,再后台保存)

## 2. 需求拆解(Kai 11 条要求 → 实现项)

| # | 要求 | 实现 |
|---|---|---|
| 1 | 保存测试结果到 Supabase | 后端 `/api/save` 改造,数据层加 Supabase 适配 |
| 2 | 自动创建数据表 | 服务器启动时自动建表(见 §4) |
| 3 | 统计表 + 角色占比 + 分布图 | 新增 `/api/stats` + 统计页 `/stats.html` |
| 4 | 记录字段:角色/标题/关键词/维度分/答案/时间 | 表结构见 §4,全部落库 |
| 7 | 首页/答题页/结果页流程不变 | 不改流程,只加保存和提示 |
| 8 | **先展示结果页,再保存数据** | 前端先渲染报告,再异步调保存 API(关键细节) |
| 9 | 保存成功轻提示 | "测试结果已保存" 小提示 |
| 10 | 保存失败不清空报告 | "结果展示成功,但保存记录失败,请稍后再试",报告保留 |
| 11 | 统计区域:总人数/各角色人数/占比/分布图 | 统计页 + 首页入口 |

## 3. 技术方案(大白话)

### 3.1 数据层:环境变量切换,保留 SQLite 兜底

```
DB_TYPE 环境变量:
  sqlite   → 本地 SQLite(现状,本地开发/无 Supabase 时用)
  supabase → 接入 Supabase(上线用)
```

- 代码里**不硬编码任何 Supabase 密钥**
- 上线时在扣子编程后台配环境变量即可,本地开发照常跑 SQLite
- 双保险:即使上线时 Supabase 没配好,网站也能用(保存到本地/跳过)

### 3.2 自动建表(启动时)

服务器启动时检测 `results` 表是否存在,不存在则自动创建(满足"自动创建数据表")。
Supabase 支持直接发建表 SQL,无需手动去后台操作。

### 3.3 表结构(一条测试记录)

| 字段 | 类型 | 内容 |
|---|---|---|
| `id` | 自增主键 | 记录编号 |
| `code` | 文本 | 结果角色(TURBO / WANDER / …) |
| `name` | 文本 | 中文名(暴走风火轮) |
| `short_title` | 文本 | 报告标题(速度是信仰,终点是执念) |
| `keywords` | JSON | 3 个关键词 |
| `dimension_scores` | JSON | 15 对维度分数 |
| `answers` | JSON | 20 题答案(每题 A/B/C) |
| `created_at` | 时间 | 完成时间(自动) |

### 3.4 保存流程(关键:不卡报告)

```
用户答完最后一题
  ↓
前端先渲染结果页(秒开,用户马上看到报告)   ← 先展示
  ↓
同时异步 POST /api/save(后台悄悄保存)      ← 后保存
  ↓
成功 → 轻提示"测试结果已保存"
失败 → 提示"结果展示成功,但保存记录失败,请稍后再试",报告不动
```

**为什么这样设计**:保存请求即使慢/失败,用户看报告的体验完全不受影响。

### 3.5 统计页 `/stats.html`

- 首页底部加一个小入口:「📊 数据统计」
- 统计页内容:
  1. **总测试人数**(大数字)
  2. **12 个角色的人数 + 占比**(列表)
  3. **角色分布图**:纯 CSS 横向条形图(不引第三方图表库,部署最稳)
- 数据来自 `GET /api/stats`:一次查出总数 + 各角色计数,后端算好占比返回

## 4. 页面改动清单

| 文件 | 改动 |
|---|---|
| `server/db.js` | 加 `supabase` 分支:自动建表 + 保存 + 统计查询 |
| `server/index.js` | `/api/save` 支持新字段;新增 `/api/stats` |
| `public/js/result.js` | 渲染报告后异步保存 + 成功/失败轻提示 |
| `public/index.html` | 首页加「数据统计」入口 |
| `public/stats.html`(新) | 统计页 |
| `public/js/stats.js`(新) | 拉取统计 + 渲染条形图 |

**不动**:答题流程、计分匹配逻辑、文案数据、设计风格(山野活力风)。

## 5. 验证标准

1. 本地无 Supabase 配置:全流程照常(SQLite),不报错
2. 配好 Supabase 后:答完题 → 报告秒开 → 保存成功提示 → 数据真的进了 Supabase 表
3. 人为断开 Supabase(模拟失败):报告仍正常展示,显示"保存失败"提示
4. 统计页:总人数正确、12 角色人数/占比正确、条形图渲染正常
5. 移动端统计页正常显示

## 6. 分工

| 谁 | 干什么 |
|---|---|
| **Hermes** | 数据层改造(Supabase 适配/建表/保存/统计 API)+ 结果页异步保存 + 验收 |
| **Codex** | 统计页前端(拉数据 + 条形图渲染 + 首页入口) |
| **Kai** | 提供 Supabase 账号/项目(见待确认 §7) |

## 7. 待 Kai 确认

1. **Supabase 账号**:注册了吗?没有的话我可以带你 5 分钟搞定(推荐选新加坡区域,离国内近,速度快一些)。注册完给我项目地址 + 访问密钥(放环境变量,不写进代码)。
2. **统计页形态**:独立页面 + 首页小入口(我的建议)?还是别的方式?
3. **本地 SQLite 保留兜底**:建议保留(没配 Supabase 时网站照样能用),你同意吗?
4. 图表用纯 CSS 条形图(不引第三方库,部署最稳),告知即可,不用专门确认。

---

*确认后开工。*
